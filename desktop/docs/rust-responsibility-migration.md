# Rust 职责迁移

目标：Rust 管理业务状态、执行编排和数据加工；WebView 保留输入、交互状态及增量渲染。
Codex `app-server` 继续拥有线程历史和执行事实，Rust 不建立第二份持久化会话数据库。

## 已实施：会话恢复元数据

`read_task` 原先从历史接口得到空的 `contextUsage`、`plan`、`pendingRequests`，前端仅对用量保留旧 Store 兜底。
WebView 重建后无法通过旧 Store 恢复这些信息。

现在 Rust 在事件投递前保存最近计划和用量；读取任务时补齐这些字段，并从原生审批注册表选择同项目、同任务的待处理请求。
窗口断开或不 ACK 不影响这份元数据；WebView 直接消费返回值，包括表示当前没有保留值的 `null`。

- 缓存最多 256 个任务、4 MiB 编码字节，单字段最多 256 KiB，按最近更新或读取淘汰。
- 超大字段清除旧值，避免恢复时展示过期计划；缓存被淘汰后须等待新的相关事件才能恢复该字段。
- WebView 重连保留缓存；Provider 重新启动清空；项目和任务删除释放对应缓存。
- 事件写入前的字节计数和克隆在 Runtime 锁外执行，文本 delta 不进入元数据缓存，也不增加 IPC 调用。
- 待审批请求复用原生注册表，不创建第二套审批状态机；最终解决或过期后不再出现在恢复快照中。

这些限制是缓存内容的编码预算，不代表 Rust/WebView 进程 RSS 上限。缓存不跨应用重启持久化。

## 已实施：失败终态错误归并

Rust 在原生事件投递前，使用同项目、同任务、同回合的不可重试 `provider.error`，补齐失败 `turn.completed` 缺失的错误正文。明确的终态错误优先；成功或中断终态不补入旧错误，重试错误不作为终态失败依据。
`read_task` 使用同一保留值补齐历史快照中的失败回合，不修改上游状态或已有错误。前端删除终态错误兜底，直接展示原生结果，包括 `null`。

- 每个任务只保留最近一轮失败原因，最多 128 个任务、1 MiB 字符串字节，单条连同项目/任务/回合身份最多 64 KiB；更新或读取按最近使用淘汰。
- 超限错误仍原样投递当前事件，但不保留恢复副本，并清除之前的旧错误；不截断或伪造上游错误正文。
- 新回合、任务删除、项目删除、Provider 重启释放对应记录；延迟到达的同轮启动事件不清除已收到的错误。
- delta 不进入失败缓存，不增加 IPC 或历史 RPC。容量淘汰、新回合开始及应用重启后，不保证恢复上游未持久化的旧错误。

## 已实施的消息子集：Skill 规范化

Rust `map_turn` 统一处理历史读取、启动响应及回合事件中的相邻 Skill 展开项：合并到前一条用户消息，保留原 ID、附件和相对顺序，Skill 按名称稳定去重；不跨活动项归并，也不吞掉包含正文或附件的独立消息。
`map_item` 与回合映射共用正文规范化，只移除已结构化 Skill 对应的连续开头 `$name` 引用，未知引用及正文内引用保持原样。

- Rust 在正文清理前为纯 Skill 展开项设置 `skillExpansion: true`，普通消息省略该字段；前端不再以规范化后的空正文猜测身份。
- 前端删除 `normalizeSubmittedSkillText` 和整回合重复扫描；提交路径只补齐本地乐观消息与附件。逐项实时展开的目标关联也已迁入 Rust。
- 回合项使用原位压缩、Skill 元数据移动，连续展开项合并后只进行一次去重和前缀扫描；回合规范化本身不新增缓存、历史 RPC 或 IPC 调用。
- 回归覆盖多个连续展开、重复 Skill、Unicode 空白、未知引用、独立 `$skill` 输入、附件保留、活动边界、原生历史/回合/单项事件一致性，以及前端提交占位和附件补齐。

## 已实施：跨事件 Skill 关联

Rust 在统一事件发布入口关联同项目、同任务、同回合内的相邻用户项，将纯展开事件替换为 `message.skills_updated`。事件明确携带目标 `itemId`，正文和 Skill 列表是该字段的最新完整值；不重传附件、不增加事件数。前端删除 `task-store-skill.ts`，仅原位更新目标用户项的 `text`、`skills`，保留 Store 身份、附件和顺序。

- 每个任务只保留最近用户项及已关联展开项的别名；最多 128 个任务、8 MiB 字符串字节，单记录最多 1,100,000 字节，Skill 和别名各最多 128 个，身份与 Skill 名称各最多 1024 字节。这不是进程 RSS 上限，也不是完整会话历史副本。
- 本机 Codex 源码 `codex-rs/core/src/session/mod.rs` 的用户项发布顺序为 `started` → `completed`。只从启动事件建立新关联；已知身份的完成事件更新原目标，没有已知身份的迟到完成原样保留，禁止猜测新目标。
- 活动项和 delta 关闭相邻归并窗口，已知别名仍可更新原项；无法关联或超过预算时原样投递。缓存淘汰、新用户项替换、身份不匹配后不保证继续归并旧展开项。
- `read_task` 只按精确身份叠加已保留 Skill，并移除已知展开别名。读取前后的项目序号、Channel 代次和 Provider 重启代次都未变化时，才允许从运行中快照末尾补种关联，避免迟到快照覆盖实时状态。
- 窗口重连保留投影，任务/项目删除及 Provider 重启释放；delta 不复制正文，用户输入准备在 Runtime 锁外完成，不新增历史 RPC。
- 回归覆盖无 WebView 恢复、原生 Channel 补丁、迟到生命周期事件、重复事件、跨项目/回合隔离、缓存预算、快照读取竞态和前端 Store/附件/顺序保留。

## 已实施：Diff 行数统计

`AgentFileChange` 和 Project Git 状态中的文件变更现在必须携带 `stats: { additions, removals }`。Rust 在 Codex 历史/回合项映射、`file_change.updated` 以及 Git 详情读取时统一计算；前端删除逐行扫描，`getFileChangeStats` 只读取这两个数字。

- Codex 新增/删除携带原始内容，空行与末尾换行按实际内容计数；更新及 Git 已跟踪文件按补丁统计。Git 未跟踪文件按实际生成的添加行计数，不能将补丁头计入新增文件行数。
- hunk 内以 `+++`、`---` 开头的代码行正常计入，文件头、模式变更和二进制提示不计入。
- Rust 借用正文逐行处理，不创建行数组，不新增常驻缓存或 IPC 请求；传输只增加固定数量字段。
- 统计只覆盖本次返回正文。实时/Git 既有截断预算保留，不推测省略内容；轻量 Git 查询仍不加载正文，详情只在 snapshot 匹配时展示。提交历史的文件导航不展示行数，正文仍按选中文件加载。
- 前端保留按展示分组求和、同路径保留最终项以及 staged/unstaged 合并；补丁规范化已继续迁移，预览解析属于渲染器职责。

## 已实施：Diff 补丁规范化

Rust 将 Codex 原始新增/删除内容、缺少头部或 hunk 的更新，以及 Git 未跟踪文本转换为补丁。`diff` 仅保存规范化结果，不另传原文；Git 已跟踪文件及提交历史已有完整补丁，继续原样传输。Modern 与 Legacy 删除 `normalizeFileChangePatch` 调用及实现，直接消费 `change.diff`。

- 原始内容即使包含 `+++`、`---` 或 `@@` 也按正文处理；保留空行、CRLF、尾部空格，无末尾换行时生成标准标记。更新补丁不再执行前端 `trimEnd()`，已有上下文前缀不会重复添加。
- 生成器借用逐行切片，按剩余预算写入；实时总正文仍不超过 512 KiB，Git 未跟踪文件保留单文件 512 KiB 和总量预算。补丁头和行前缀计入预算，不新增 IPC 或常驻缓存。
- 合成 hunk 截断时只保留完整行，并按保留内容重建行数；连文件头都无法容纳时返回空正文。完整上游补丁仍可能被既有预算截断，预览器继续承担不完整补丁的容错；统计不推测省略内容。
- 实时 `originalByteLength` 表示上游原文大小，生成头部使结果超限也设置 `truncated`。语法高亮、预览解析和虚拟行布局继续由渲染器处理。

## 已实施：队列相邻移动

`move_queued_submission` 替代 WebView 的全量重排命令；前端只提交 `queuedSubmissionId` 和 `offset: -1 | 1`。Rust 根据当前队列计算相邻交换，条目已出队或无相邻项返回 `{ moved: false }`，不执行写入。前端删除列表复制、索引查找和交换逻辑，三种结果（移动、无变化、失败）均刷新该任务的队列查询，错误继续向调用方传播。

- 读取最多 100 页、每页 100 项，累计 ID 正文最多 256 KiB，单个 ID/游标最多 4096 字节；拒绝重复 ID、空或重复游标、超限页面。读取与重排共用 30 秒超时，不因分页倍增等待时间。
- 原生只反序列化 ID，不再映射队列提示正文、附件和 Skill；不新增常驻队列副本。WebView 写请求由全量 ID 数组变为固定字段，但新增了按需原生分页读取，Codex 的 list 响应仍含正文，不能宣称端到端延迟或 Provider 传输量下降。
- Codex 0.154.0 的 `state/src/runtime/queued_items.rs::reorder` 在事务内校验完整 ID 集合。读取后发生增删会被拒绝，本应用不额外重试；该接口没有顺序版本或 CAS，同一集合的外部并发重排仍可能后写覆盖前写。
- 新命令只授权主窗口，旧 WebView 重排入口、客户端方法和协议 Schema 已移除。相对移动不提供虚假的幂等键参数；完整提交幂等与队列确认状态仍待迁移。

## 已实施：队列完整读取

`list_queued_submissions` 只接受任务作用域，响应改为 `{ data }`；Rust 在一次任务校验后完成分页。前端删除 `listAllQueuedSubmissions` 循环，直接消费 `AgentQueuedSubmissionSnapshot`，旧分页 Schema 和 Cursor 参数同步移除。N 页队列的 WebView 读取由 N 次 invoke 降为一次；Provider 仍需 N 次分页请求，但任务归属校验不再逐页重复。

- 最多 100 页、每页 100 项；分页共用 30 秒超时。按序列化后的 JSON 字节限制完整结果为 4 MiB，包括转义、信封、逗号和编辑状态补齐；计数不分配第二份 JSON 正文。拒绝重复 ID、空/重复游标、超过 4096 字节的游标和超大页，任何失败都丢弃部分结果。
- 编辑状态在全部页面读取完成后补齐，避免第二页的编辑项被第一页误清。只有与读取前相同且仍缺失的编辑项才清理，读取期间切换到其他条目的新编辑状态保留。
- 未新增常驻快照缓存；这里的完整结果仅表示已遍历所有页面，并非 Provider 原子快照。外部并发增删导致的分页一致性仍受官方接口约束；实际端到端延迟和真实 WebView 内存尚未测量。

## 已实施：任务创建幂等

前端原有任务创建键现在通过 `start_task` 传入 Rust。`TaskCreationRegistry` 在执行创建前登记身份，同项目同键共享一次创建及临时工作区绑定，返回同一成功摘要或原始错误；跨项目复用键直接拒绝。重放摘要不是任务当前状态，后续读取仍以 Provider 为准。

- 记录位于 `AppState`，跨 WebView 重建和 Provider 重启保留，不跨应用重启。保留窗口从注册时起计算 15 分钟；在途记录即使过期也不淘汰。过期完成项在下次请求时清理，窗口外同键可能重新创建。
- 最多保留 128 项，容量耗尽在执行副作用前拒绝。键和项目身份分别限制为 128、1024 字节；单项结果字符串正文或错误 JSON 最多 8 KiB，超大结果保留固定不确定错误，预算不代表进程 RSS 上限。
- 每次调用最多等待 120 秒，超时或调用方取消只结束等待，创建与工作区收尾继续；后续同键可取得最终结果。工作异常退出保留不确定记录，不接管重跑。
- 失败在保留窗口内也会重放，不能通过同键自动重试创建；新尝试需要新键，并先核对任务列表以避免重复。完整失败重试交互、创建后启动首轮的恢复编排尚未迁移；Turn 启动幂等见下文。

## 已实施：启动前线程恢复

本批补齐启动前的原生线程恢复决策：删除前端 `threadAlreadyLoaded` 和 IPC `resumeTask`，普通提交、Goal 与定时任务由 Rust 按当前 Provider 状态确认写入权，复用打开任务时的同一规则与本次已读取偏好。先调用 `thread/resume(excludeTurns:true)` 恢复订阅；只有精确的目标线程缺少 rollout 错误才回退读取轻量线程元数据，验证项目、线程和载入状态。失败不启动 Turn，不额外重试业务操作；现有传输层对过载错误的有界重试保持不变。

这消除了创建结果重放或前端状态过期导致的恢复判断，但尚未合并“创建并启动”事务或实现部分成功后的完整恢复。此恢复步骤未新增常驻缓存或 WebView 请求；无 rollout 新线程相比此前跳过恢复的路径增加一次失败恢复 RPC 和一次轻量读取，真实端到端延迟尚未测量。

## 已实施：Turn 启动幂等

前端已有的启动键通过 `start_turn` 传入 Rust，普通与 Goal 提交在副作用前登记请求；同键同内容共享一次启动并重放结果，任务、项目、提示词、附件、Skill 或启动选项改变时拒绝复用键。以反序列化后的完整输入流式计算 SHA-256，不缓存提示词副本；重复请求仍需计算指纹，但不重复恢复线程、写入设置或启动 Turn。

- `AppState` 内最多 128 条记录，从注册起保留 15 分钟，未结束工作即使过期也不淘汰；完成后按需回收。应用重启丢失记录，窗口外或不同键可能再次执行。
- 单次输入 JSON 编码限制 4 MiB，在途输入合计限制 8 MiB；项目和任务身份分别最多 1024 字节，键最多 128 字节。缓存结果为共享编码字节，每项最多 64 KiB；超限立即停止编码并保留 `TURN_START_UNCERTAIN`。这些编码预算不是进程 RSS 上限。
- 每个调用最多等待 120 秒，取消或超时只结束等待，后台工作继续；后续同键可取得结果。工作异常退出及超大结果保留不确定记录，不接管重跑。原始 RPC 错误和字符串错误保留原结构。
- 失败也会重放，完整失败恢复交互尚未迁移；用户开始新尝试前仍需核对任务状态并使用新键。响应是原启动结果及 checkpoint，不代表当前 Turn 状态；创建与启动尚未形成整体恢复流程。定时任务、steer、排队和 Review 不经过此注册表；未改变底层过载重试规则。

## 已实施：运行时未就绪后的同键恢复

原生创建和 Turn 启动原先将 `CodexRuntimeUnavailable` 当作永久结果重放，Provider 恢复后仍会卡在旧失败。本批仅对该原生类型开放下一次同键请求的重试：当前创建与启动路径在取得连接之前返回它，因此尚未发送创建或执行请求。前端继续提交原键，无需推断错误类别或生成替代键；没有增加后台自动重试。

重试保留原项目、请求指纹和最初登记时间，在锁内替换已完成尝试；并发重试只启动一次，旧等待者仍收到原错误。满容量允许替换本键，但仍检查在途输入预算。RPC 即使返回相同文案也不能开启重试；其他错误、超时及不确定结果仍重放。此前各节的失败保留规则以此例外为准；尚未处理写入占用等其他执行前失败，也未完成“创建并启动”的整体恢复。

## 已实施：普通提示词提交的原生顺序编排

普通新任务与已有任务统一调用 `submit_prompt`，前端不再串联 `startTask` 与 `startTurn`。Rust 在创建前校验幂等键和输入编码预算，复用已有创建、启动登记表；创建成功后通过 Channel 立即发送任务摘要，最终响应再次携带 `createdTask`，并以 `started` / `failed` 区分启动结果。前端只消费摘要与结果，同一次调用按任务 ID 去重通知；通知丢失时先从最终响应补回任务，再展示启动错误。

入口最多允许 16 个在途提交，输入编码合计最多 8 MiB，单请求沿用 4 MiB 上限；容量不足立即拒绝，不排队持有输入。许可覆盖创建等待阶段，调用结束自动释放，各阶段 worker 仍由原登记表限制。预算衡量编码大小，不等于进程实际内存；创建前校验和启动登记各计算一次指纹，尚未实测性能收益。提交专用协议校验在发送时按需加载，避免增加工作台首次加载体积。

这是顺序编排，不是原子事务或持久化提交日志。创建与启动仍分别使用原键及各自保留窗口；创建结果不确定、通知和最终响应均丢失、应用重启后的整体恢复仍未解决。Review 的后续迁移见下一节，steer 与排队编排仍待迁移。

## 已实施：Review 提交编排与启动去重

Review 改为单次 `submit_review`，移除前端创建后启动的顺序控制和未接收幂等键的旧 `start_review` 命令。Rust 在创建前检查目标类型、必需字段、字段白名单、身份与编码预算；创建成功立即通知，Review 失败仍在最终响应保留任务摘要。普通提交与 Review 共用客户端通知去重、失败补回和 Channel 释放逻辑，专用校验按需加载。

创建继续复用创建登记表，Review 使用独立的 `TurnStartRegistry` 实例，以项目、任务及完整目标指纹校验同键请求。最多 128 项记录、保留 15 分钟、在途编码输入最多 8 MiB、单项结果最多 64 KiB；同键同内容重放，目标变化拒绝，取消等待不取消已登记 worker。仅原生 `CodexRuntimeUnavailable` 允许同键再次取得连接，其他失败与不确定结果沿用 Turn 登记规则。

普通提交与 Review 共享入口的 16 个在途调用、8 MiB 编码输入预算，避免等待创建阶段各自扩大占用。Review 仍沿用原有 `thread/read` 后 `review/start` 的 Provider 路径；本批不新增线程恢复规则，也不提供创建与 Review 的跨重启原子恢复。Review 引用分支和未提交改动等可变仓库状态，同键重放的是原启动结果，不会重新审查其后发生的改动。

## 已实施：Steer 提交幂等

前端原有 Steer 键现在通过 `steer_turn` 传入 Rust，客户端接口与协议均要求显式键。Rust 对项目、任务、目标 Turn 和完整输入流式取指纹；附件和 Skill 元数据也参与校验，登记后才解析附件并发送 `turn/steer`。同键同输入重放，不再次解析附件或发送指令；同键更改目标回合或输入直接拒绝。保留上游 `expectedTurnId` 校验，不自动把追加操作转换为新回合。

Steer 复用独立 `TurnStartRegistry` 实例，最多 128 条记录、保留 15 分钟、在途编码输入最多 8 MiB、单项结果最多 64 KiB；单项输入最多 4 MiB。与普通提交和 Review 共享入口的 16 个在途调用、8 MiB 输入预算，重复等待者也占用额度；调用返回或取消时释放入口许可，登记 worker 仍受登记表预算约束。预算衡量编码大小，不代表真实进程内存。

只有原生 `CodexRuntimeUnavailable` 允许后续同键重试，RPC 同名文案不能触发；其他错误、不确定结果与等待超时沿用 Turn 登记规则，取消等待不取消 worker。`TURN_START_UNCERTAIN` 在此表示追加结果不确定，应先核对任务，不能自动换键重发。本批不迁移前端 start/steer/queue 决策或“追加成功后确认队列”的整体编排，也不覆盖跨重启恢复；同键重放不保证原目标回合当前仍运行。

## 已实施：队列启动幂等

`start_queued_submission` 现在要求显式幂等键，Rust 在读取任务、发送 `thread/queue/start` 和清除编辑状态之前登记项目、任务及可选队列项身份。同键重放原 Turn；未指定项的“启动下一项”也不能在重试时再次消费队列。同键更改项目、任务或指定项拒绝；同一项后来内容变化不使旧键自动成为新尝试。

队列启动采用独立有界登记表，沿用 128 条记录、15 分钟保留、64 KiB 单项编码结果、120 秒等待及原生运行时未就绪同键重试规则，并共享 16 个在途调用和 8 MiB 编码输入入口预算。取消等待不取消已登记 worker，重放不重新清除编辑状态。客户端透传键，队列按钮以 ref 保留最近一次失败尝试，成功或更换目标后释放/替换；不增加渲染状态或前端执行缓存。

Codex 0.154.0 的 `thread/queue/start` 仅支持空闲且已加载的任务，仍由上游检查是否可启动。本批保持原有任务读取路径，不增加自动恢复、忙时转 Steer 或队列内容复制；运行中队列的“追加 → 删除”编排及跨重启恢复仍待迁移。`TURN_START_UNCERTAIN` 不能当作启动失败自动换键，前端 ref 也不跨卸载、重启或多个目标保存全部尝试。

## 第十七批：运行中队列追加与清理编排

`steer_turn` 接收可选 `queuedSubmissionId`。有队列身份时，Rust 在共享入口预算内执行追加，再删除该项并清除对应编辑状态；前端仅提交意图和刷新队列，不再单独发起删除。项目、任务、目标 Turn、完整输入和队列项 ID 共同绑定幂等键。

追加阶段采用独立有界登记表，沿用 128 条、15 分钟保留和既有输入/结果限制。删除失败返回 `QUEUE_CLEANUP_FAILED`，明确输入已接受；保留窗口内同键同输入重试仅重放追加结果、补做删除。追加失败或结果不确定不删除，队列项已经不存在也算清理成功。协调任务持有入口预算，取消调用方等待不会中断后续清理。

这是顺序操作，不是原子消费：上游没有追加并消费队列的接口，删除失败期间队列项可能仍被自动执行；并发编辑、其他客户端消费、超过保留窗口及应用重启后的恢复不在本批保证范围内。输入仍来自当前界面队列快照，没有增加内容 CAS。前端仅保留当前尝试的键，不能跨卸载或目标回合变化保证补偿重试。

## 第十八批：运行中队列清理的原生恢复身份

Rust 按项目、任务和队列项建立独立恢复记录，将完整输入摘要与已接受结果绑定。追加已确认后，同内容以新幂等键、不同目标回合再次调用队列 `steer_turn`，仅补做清理；保留中的原键仍不能改动请求身份。输入变化返回 `IDEMPOTENCY_CONFLICT`，不再次追加或删除。恢复返回 `cleanupOnly: true`，前端不再生成当前回合的本地待确认消息。

同一队列项的协调任务串行执行，最多保留 128 项，按首次登记保留 15 分钟；调用方与追加 worker 持有租约时不回收。恢复记录只保存摘要、原键和最多 64 KiB 的编码结果，不复制提示词正文；入口继续共享 16 个在途调用和 8 MiB 编码输入预算。恢复结果额外编码存储最多约 8 MiB，不代表进程 RSS 上限。锁等待最多 120 秒，取消前端等待不取消协调任务。

未确认追加成功时，新键不能取代原尝试，返回 `TURN_START_UNCERTAIN`；原键仍沿用底层结果重放和原生运行时未就绪重试规则。不增加后台自动重试。此保证仅适用于运行中队列的 `steer_turn` 路径，不覆盖切为空闲后的 `queue/start`、上游自动消费、跨应用重启或超过保留窗口。清理没有内容 CAS，其他客户端修改队列的竞态仍存在。

## 第十九批：空闲队列启动与清理恢复

`start_queued_submission` 在 Rust 中读取已有追加恢复记录；已接受的队列项转为清理决策，返回 `{ cleanupOnly: true, taskId, queuedSubmissionId }`，不发送 `thread/queue/start`。清理决策与原队列启动键绑定，删除失败后同键只补清理，不重新选择下一项。前端按响应分支刷新队列，不构造虚假的新 Turn 或本地待确认消息。

未指定队列项时，Rust 使用既有有界队列读取固定当前首项，再判断启动或清理；空队列返回 `QUEUE_EMPTY`，不会回退到未指定目标的上游启动。指定普通项不额外读取全队列。清理前再次验证任务归属，持有追加恢复租约和同项协调锁，重新读取当前队列并核对完整输入摘要；内容变化返回 `IDEMPOTENCY_CONFLICT`，项已不存在视为清理完成，未确认接受则返回 `TURN_START_UNCERTAIN`。未知结果不能自动换键重发。

入口预算覆盖决策及清理，取消前端等待不取消后台协调。复用 128 项、15 分钟的恢复记录和既有队列读取限制，不增加前端恢复缓存或常驻轮询；未指定项的首次选择及清理核对会增加原生队列读取。结果或恢复记录超过保留窗口、应用重启、外部编辑/自动消费以及读取到写入之间的竞态仍不保证原子性。输入摘要严格匹配，附件元数据变化也可能要求手动核对；本批没有放宽为模糊内容匹配。

## 第二十批：启动与追加共用消费租约

Rust 为同项目、任务、队列项的空闲启动与运行中追加共用消费租约，关闭选择完成到发送 RPC 之间的并发缺口。启动 worker 持锁直到 RPC 和原生收尾完成；租约释放后仍保留启动尝试标记，防止响应未知时换键追加或再次启动。原启动键继续重放原结果，已接受追加项继续只补清理，前端不新增协调状态。

两条路径共用最多 128 项、15 分钟的恢复表，在途租约不淘汰；不额外保留提示词正文。消费前容量不足使用原生 `QueueRecoveryCapacityExceeded`，容量释放后允许同键同身份重试，并保留原身份和登记时间；这是运行时未就绪之外新增的安全重试条件，不匹配 RPC 错误文案，不自动重试。传输失败或 RPC 拒绝仍保留原尝试。

保证仅覆盖同一应用、保留窗口内的本地入口，不覆盖上游自动消费、外部客户端、内容 CAS 或应用重启。此边界补充第十八、十九批，不代表上游提供原子队列消费。

## 第二十一批：Goal 输入校验

第二十一批已将 Goal 输入校验统一到 Rust：去除首尾空白后目标非空、最多 4000 个 Unicode 标量且不接受附件或 Skill，与 Codex 0.154.0 `validate_thread_goal_objective` 的计数方式一致。修正前端按 UTF-16 长度误拒绝 emoji 目标的问题；前端只按原生错误码展示文案，保留通用空输入禁用等交互状态。

普通提交创建前、直接启动登记前、计划任务保存和执行前调用共用规则，避免无效 Goal 先创建线程、解析附件或写入任务设置。低层 `thread/goal/set` 复用目标校验，普通提示词仍沿用原限制；不增加 IPC、目录读取、后台缓存或依赖，字符扫描超限即停且不复制正文。前端已上传的附件不在该校验的回收范围内。

本批未迁移异步问题的历史文本匹配；该职责仍需连同稳定回答身份、历史分页和实时事件对账一起处理，不能以此批输入校验替代。

## 第二十二批：待处理请求回答幂等

第二十二批补齐 `resolve_pending_request` 的原生幂等：前端此前创建了重试键，但客户端忽略该键；现在透传原键与 Project/Task/Turn/Item/requestId/createdAt，Rust 以完整回答登记，核对原生记录后才消费请求。成功、失败及插件安装都由同一 worker 编排，取消 WebView 等待不再中断回答，重复同键只重放原结果。

回答入口独立允许 16 个请求、8 MiB 编码输入，避免等待审批的普通提交占满控制流预算；复用 128 项、15 分钟、4 MiB 单输入、64 KiB 结果与 120 秒等待限制。在途记录不淘汰，回答 JSON 通过 `Arc` 共享，不为 worker 再深拷贝一份。只在未取得运行时连接时允许原键重试，RPC/写入失败仍重放；连接和待处理记录在同一锁内取得，旧连接失败不恢复记录、旧连接成功不发布完成。

此批不迁移 `agentMessage.questions` 的历史文本匹配，也不包含前端打开 MCP URL 的副作用去重。JSON-RPC 回答没有接收确认，本地写入不能视为上游已接受；不同键、保留窗口之外、应用重启或上游自行结束请求不具备整体事务保证。

## 第二十三批：用户输入回答原生校验

`item/tool/requestUserInput` 的回答在 Rust 写入 JSON-RPC 前，以原生待处理题目为准校验完整性、题目身份和单项非空字符串。拒绝漏答、额外答案、重复题目 ID、畸形类型及纯空白回答，不再依赖前端按钮状态保证有效性。校验只借用输入，保留合法自由文本和秘密值的原始字节，不增加 IPC 或常驻状态。

前端继续承担草稿、表单提示和提交展示；本批未改动前端现有输入修剪行为。每题单项是 Codexly 桌面端当前表单契约，上游 `RequestUserInputAnswer.answers` 本身允许字符串数组；不新增选项标签白名单。异步 `agentMessage.questions` 的历史文本对账仍未迁移。

验证：新增 3 项原生回归，RED 复现非法答案及重复题目被接受，GREEN 全部通过。`pnpm check:rust` 通过格式、Clippy、571 项单元测试、6 项集成测试和 3 项既有性能基线；真实 Codex 0.154.0 生命周期通过。本批没有前端改动，未重跑浏览器测试，也未实测真实问答端到端链路或新增校验的延迟收益。

## 第二十四批：权限审批回答原生校验

Rust 对 `grantedPermissions` 校验类型、类别、唯一性和最多两项的集合限制，`scope` 仅允许 `turn`、`session`。删除静默过滤非字符串的旧逻辑；只从原生请求提取被选中的权限配置，拒绝未请求的权限，空集合仍表示不授予权限。权限响应拆为独立模块，前端继续提供选择与结果展示，不增加 IPC 或常驻状态。

新增原生回归覆盖非法类别、非法作用域、未请求权限及两种作用域下的空集/单项/完整集合精确响应。MCP form 内容校验和异步问答历史对账不在本批范围。

验证：4 项定向回归从 RED 转为 GREEN；`pnpm check:rust` 通过格式、Clippy、575 项单元测试、6 项集成及 3 项既有性能基线，真实 Codex 0.154.0 生命周期通过。未改动前端，未重跑浏览器测试；生命周期测试不代表真实权限审批端到端交互已覆盖。

## 第二十五批：删除不可达的队列编辑流程

重新核对调用链后确认，当前编辑入口已采用“删除原队列项 → 恢复完整输入”的流程，生产调用没有将队列项设为 `editing` 的入口。因此撤销此前为“更新后启动”新增原生编排的建议，直接移除旧更新命令、ACL、客户端与协议类型、原生编辑登记表及前端锁定/自动启动分支。保留撤回编辑、队列移动、空闲启动和运行中追加行为；删除旧编辑状态专用测试，保留分页与内容回归。

本批是消除冗余，不新增 Rust 副本或 IPC。队列新增的幂等键透传、可靠消息身份对账仍未实现；不能把这次清理视为这些问题已解决。后续应优先处理有实际调用的业务路径，表单交互、渲染缓存与帧调度保留在前端，图片处理先测量再决定。前文各批中的编辑状态描述属于历史实现，本节取代其现行规则。

验证：旧命令授权回归先失败后通过；`pnpm check` 的前端检查、354 项测试、Modern/Legacy 构建和体积预算通过，Rust 阶段发现未使用导出，清理后 `pnpm check:rust` 全部通过（573 项单元、6 项集成、3 项既有性能基线）。Chromium/WebKit 队列及普通提交共 30 项通过，真实 Codex 0.154.0 生命周期通过。移除两个仅服务于旧登记表的单测；工作台加载体积 1,454,434 字节，未放宽预算。未实测真实权限/队列交互或此次清理的延迟收益。

## 第二十六批：队列新增幂等

`add_queued_submission` 必须携带幂等键；Rust 使用独立有界登记表，按项目、任务、客户端消息身份和完整输入校验同键请求，重放已有结果。登记后的工作不随调用方取消而取消，与普通提交共享 16 个请求、8 MiB 输入预算。沿用单输入 4 MiB、128 项、15 分钟及单结果 64 KiB 的登记限制；超大结果返回不确定状态，不重新执行。仅未取得运行时连接允许原键重新执行，未知错误不自动重试。

前端仅保留当前尝试身份，同时作为消息 ID 与幂等键；请求或缓存刷新失败保留，完整成功后清除。下一次成功后的相同输入使用新身份。此机制不跨应用重启，也不保证新键或登记过期后的去重；可靠消息历史对账仍未迁移。

验证：前端重试与客户端透传回归先失败后通过；`pnpm check` 全部通过（354 项前端、577 项 Rust 单元、6 项集成及 3 项既有性能基线，7 项默认忽略）。Chromium/WebKit 队列及普通提交共 32 项通过，真实 Codex 0.154.0 生命周期通过。新增 4 项原生回归覆盖完整身份冲突、结果重放、取消等待后继续执行及预算保留、无效身份和安全重试边界。生命周期测试不经过新登记表，尚未实测真实队列幂等全链路或本次改动的延迟收益；收尾未发现需要额外抽象的重复逻辑。

## 后续迁移边界

| 顺序 | 待迁移职责 | 验收重点 |
|---|---|---|
| 2 | 消息身份、终态及快照对账规则 | 先建立可靠原生身份，再删除前端文本猜测；保留前端增量 Store、局部订阅与帧调度 |
| 3 | MCP 回答最终校验、调度时区与规则转换 | 校验和业务语义由 Rust 统一；表单草稿、即时提示与本地化留在前端 |
| 条件满足后 | 异步问题回答关联、队列确认状态 | 上游有可靠关联身份或明确有界恢复方案后实施，不仅将启发式匹配换语言 |
| 测量后决定 | Diff 正文按需读取、图片软件加工和缓存 | 必须证明减少传输或主线程耗时，且不恶化总内存；不以减少前端行数为目标 |

当前已迁移恢复元数据、失败终态错误规则、Skill 规范化及有界跨事件关联、Diff 行数统计与补丁规范化、队列相邻移动及完整读取、任务创建与 Turn 启动幂等、启动前线程恢复，没有迁移完整 `task-store-events.ts`、通用消息身份对账、前端事件历史和恢复重试器；不能视为主工作台已成为纯渲染层。
快照元数据和 checkpoint 在同一 Rust 临界区读取，但这不代表上游多个历史 RPC 与实时正文事件已经形成原子快照。

## 验证

2026-09-13 第二十二批：`pnpm check` 通过，354 项前端测试、568 项 Rust 单元测试、6 项集成、3 项既有性能基线通过，7 项默认忽略；类型、格式、Clippy、Modern/Legacy 构建及体积预算通过。新增 9 项原生回归覆盖结果重放、完整身份冲突、取消等待后继续执行及许可保留、执行前拒绝、运行时未就绪恢复、单次 JSON-RPC 写入、独立控制预算和旧连接隔离；客户端定向 10 项通过，RED 复现丢失重试键与同键重复执行。Chromium/WebKit MCP 交互 10 项、真实 Codex 0.154.0 生命周期通过；未实测真实 WebView 回答的取消/重试链路、插件安装重放或新增校验的延迟收益。

2026-09-13 第二十一批：`pnpm check` 的前端检查、354 项测试、Modern/Legacy 构建及体积预算通过；新增测试曾误引入不存在的 `uuid` 导致 Rust 编译失败，改用标准库后完整 `pnpm check:rust` 通过，559 项单元、6 项集成、3 项既有性能基线通过，7 项默认忽略，格式及 Clippy 通过。工作台加载体积为 1,455,394 字节，未提高预算。Chromium/WebKit 普通提交交互 16 项和真实 Codex 0.154.0 生命周期测试通过。新增原生回归验证无效 Goal 在创建/计划保存前拒绝、Unicode 边界及无磁盘初始化，前端覆盖原生错误展示及 emoji 目标透传；未实测真实 WebView Goal 执行或延迟收益。收尾未发现需要额外抽象的重复逻辑。

2026-09-13 第二十批：`pnpm check:rust` 通过，555 项单元测试、6 项集成测试、3 项既有性能基线通过，7 项默认忽略；格式与 Clippy 通过。真实 Codex 0.154.0 安装与生命周期测试单独通过。新增 3 项回归覆盖启动选择后追加拦截、同项锁等待与过期租约保护、消费前容量恢复后原键重试；RED 分别复现重复追加及容量错误被永久缓存，最终全量 Rust 测试转绿。本批未修改前端，未重跑浏览器或构建；真实 WebView 并发队列消费及本次延迟收益未实测。

2026-09-13 第十九批：`pnpm check` 通过，350 项前端测试、552 项 Rust 单元测试、6 项集成和 3 项既有性能基线通过，7 项默认忽略；Modern/Legacy 构建、类型检查、格式、Clippy 与体积预算通过，工作台加载体积为 1,455,439 字节。Chromium/WebKit 队列交互 14 项、真实 Codex 0.154.0 生命周期测试通过。RED 复现清理回调未执行、已接受项仍选启动，以及前端把清理结果当新 Turn；原生覆盖清理重试固定原项、取消等待、预算、内容冲突、缺失项、首项选择、空队列及未知结果隔离。最终收紧明确启动身份后再运行原生定向与静态检查；未实测真实模型从运行中转为空闲的队列恢复或新增 RPC 延迟。

2026-09-13 第十八批：`pnpm check` 通过，349 项前端测试、542 项 Rust 单元测试、6 项集成和 3 项既有性能基线通过，7 项默认忽略；格式、Clippy、类型检查、Modern/Legacy 构建及体积预算通过，工作台加载体积为 1,455,251 字节。原生队列追加与恢复定向 12 项通过，Chromium/WebKit 队列交互 12 项通过，真实 Codex 0.154.0 生命周期测试通过。RED 分别复现换键换回合重复追加和清理重放重复投影；GREEN 覆盖并发、内容冲突、未知结果、项目/任务隔离及容量回收。未实测真实 WebView 重建后的模型队列恢复、跨回合清理延迟或进程内存峰值。

2026-09-13 第十七批：`pnpm check` 通过（346 项前端、535 项 Rust 单元测试、6 项集成和 3 项性能基线，7 项默认忽略）。随后补充的客户端透传测试与原生传输顺序测试通过：客户端定向 10 项、原生队列追加定向 6 项；最后重新执行 lint、类型检查与全目标 Clippy 均通过。Chromium/WebKit 队列交互 12 项通过，真实 Codex 0.154.0 生命周期测试通过。新增覆盖清理失败同键重试、拒绝不删除、身份冲突、取消等待后继续清理和已不存在的队列项；未实测真实模型并发队列消费或新增编排的端到端延迟。

2026-09-13 第十六批队列启动幂等的 `pnpm check` 通过：346 项前端测试、530 项 Rust 单元测试、6 项集成测试和 3 项既有性能基线通过，7 项默认忽略；Modern/Legacy 构建、类型检查、Clippy、格式和体积预算通过。工作台加载体积为 1,454,956 字节，未提高预算。

新增 7 项原生回归覆盖下一项重放、身份冲突、无效键/ID、共享容量、运行时恢复、取消等待及重复调用只发送一次 `thread/queue/start`。新增协议测试，客户端透传测试通过；Chromium/WebKit 队列交互共 10 项通过，覆盖响应丢失后按钮复用原键。真实 Codex 0.154.0 安装与生命周期测试通过，但不经过新队列启动登记；真实队列消费端到端与性能收益尚未实测。

2026-09-13 第十五批 Steer 幂等迁移的 `pnpm check` 通过：345 项前端测试、523 项 Rust 单元测试、6 项集成测试和 3 项既有性能基线通过，7 项默认忽略；Modern/Legacy 构建、类型检查、Clippy、格式与体积预算通过。工作台加载体积为 1,454,565 字节，未修改既有预算。

新增 7 项原生回归覆盖完整身份冲突、无效键/输入、共享预算回收、运行时未就绪恢复与 RPC 失败重放、取消等待及重复请求仅发送一次 `turn/steer`；新增协议校验测试，并更新客户端原键透传断言。Steer 相关前端定向 14 项通过，真实 Codex 0.154.0 安装与生命周期测试通过。真实生命周期不经过新 Steer 登记，未实测真实模型追加或原生 WebView 幂等全链路；既有性能基线不能证明此次迁移的延迟收益。

2026-09-13 第十四批 Review 提交编排的 `pnpm check` 通过：344 项前端测试、516 项 Rust 单元测试、6 项集成测试及 3 项既有性能基线通过，7 项默认忽略；Modern/Legacy 构建、类型检查、Clippy、格式与体积预算通过。工作台加载体积为 1,454,359 字节，未调整 1,456,000 字节预算。最后精简测试 fixture 后，提交定向测试与类型检查再次通过。

新增 7 项原生回归覆盖目标/身份/预算校验、同键冲突、结果与失败重放、取消等待、原生运行时恢复及重复请求只发送一次 `review/start`；新增 4 项客户端测试和 1 项前端编排测试。共用通知逻辑的 Chromium/WebKit 普通提交交互共 16 项通过，真实 Codex 0.154.0 安装与生命周期测试通过。浏览器回归未直接操作 Review 菜单，真实生命周期测试不经过新命令；真实 WebView Review 全链路与延迟仍未实测。

第十三批完整 `pnpm check` 已通过，包括 Modern/Legacy 构建、类型检查、Clippy、格式检查、6 项集成测试及 3 项既有性能基线。按需加载修正后工作台加载体积为 1,453,905 字节，低于原定 1,456,000 字节预算，未调整阈值。

2026-09-13 第十三批普通提交编排：339 项前端测试、509 项 Rust 单元测试通过，7 项默认忽略；新增 6 项原生回归覆盖创建通知顺序、部分失败、已有任务、前置校验、数量/字节预算回收和复用双阶段幂等登记。新增 5 项客户端回归覆盖通知去重、最终响应补回、协议校验与回调释放；Chromium/WebKit 共 16 项提交交互测试、真实 Codex 0.154.0 安装与生命周期测试通过。浏览器使用原生客户端替身，真实生命周期测试不经过 `submit_prompt`，尚未实测新入口在真实 WebView 与模型执行中的端到端行为或延迟。

2026-09-13 第十二批运行时未就绪恢复的 `pnpm check` 通过：334 项前端测试、503 项 Rust 单元测试、6 项集成测试、3 项既有性能基线通过，7 项默认忽略；构建、类型检查、格式检查、Clippy 和体积预算通过。新增 8 项原生回归覆盖同键恢复、身份与容量约束、原登记时间、并发重试、旧等待者隔离及 RPC 错误文案不触发重试。真实 Codex 0.154.0 私有安装与生命周期测试通过，但未实测真实 WebView 在 Provider 恢复期间重新提交的端到端流程。

2026-09-13 第十一批 Turn 启动幂等迁移的 `pnpm check` 通过：334 项前端测试、495 项 Rust 单元测试、6 项集成测试和 3 项既有性能基线通过，7 项默认忽略；Modern/Legacy 构建、类型检查、格式检查、Clippy 与体积预算通过。新增 14 项原生回归覆盖请求指纹、结果重放、并发及取消等待、零等待超时、容量、过期、编码预算和 worker 异常；原生协议替身验证重复提交只发送一次 `turn/start`。前端定向 11 项通过；真实 Codex 0.154.0 私有安装与生命周期测试通过，但不经过新注册表。未实测真实模型重复提交、Goal 端到端幂等或新增指纹计算的端到端延迟。

2026-09-13 第十批启动前线程恢复迁移的 `pnpm check` 通过：333 项前端测试、481 项 Rust 单元测试、6 项集成测试与 3 项既有性能基线通过，7 项默认忽略；构建、类型检查、格式检查、Clippy 和体积预算通过。原生回归验证无 rollout 新线程继续启动、未载入线程拒绝、项目/线程身份隔离及写入占用错误保留；前端 10 项定向测试通过。真实 Codex 0.154.0 生命周期测试通过，其中验证共用恢复规则的新线程与跨进程占用路径；未实测真实模型 Turn 执行、Goal/定时任务端到端恢复或新增 RPC 的延迟。

2026-09-13 第九批任务创建幂等迁移的 `pnpm check` 通过：333 项前端测试、476 项 Rust 单元测试、6 项集成测试和 3 项既有性能基线通过，另有 7 项测试默认忽略；Modern/Legacy 构建、类型检查、格式检查、Clippy 和体积预算通过。新增 8 项原生回归覆盖重放、并发及取消等待、失败、身份校验、容量与过期、超大结果和工作异常退出；前端协议、客户端与提交定向测试共 11 项通过。真实 Codex 0.154.0 私有安装与 app-server 生命周期测试通过，但该生命周期测试不经过新增注册表；未实测 120 秒等待超时、应用重启恢复或真实 WebView 端到端创建幂等。

```sh
cargo test --manifest-path src-tauri/Cargo.toml --lib snapshot --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib failure_projection --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib conversation_ --locked
pnpm exec vitest run src/features/conversation/runtime/task-store-context-usage.test.ts
pnpm exec vitest run src/features/conversation/runtime/task-store-terminal-error.test.ts
cargo test --manifest-path src-tauri/Cargo.toml --lib skill --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib file_change_stats_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib file_patch_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib native_queue_move_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib queue_snapshot_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib task_creation_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib should_retry_after_runtime_becomes_available --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib retry_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib application::turn_start --locked
pnpm exec vitest run src/protocol/turn-start.test.ts src/platform/tauri/sidebar-client.test.ts src/features/workbench/composer-state-submission.test.ts
cargo test --manifest-path src-tauri/Cargo.toml --lib turn_readiness_should --locked
cargo test --manifest-path src-tauri/Cargo.toml --lib conversation_command --locked
pnpm exec vitest run src/protocol/task-creation.test.ts src/platform/tauri/sidebar-client.test.ts src/features/workbench/composer-state-submission.test.ts
pnpm exec vitest run src/features/conversation/runtime/task-store-skill-update.test.ts src/features/conversation/runtime/task-runtime-submission.test.ts
pnpm exec vitest run src/features/diff/file-change.test.ts src/features/workbench/components/workbench-inspector-git-status.test.ts
pnpm exec vitest run src/platform/tauri/queue-command-contract.test.ts src/platform/tauri/sidebar-client.test.ts
pnpm exec vitest run --config vitest.browser.config.ts src/features/workbench/hooks/use-composer-queue.browser.test.tsx
pnpm check
```

原生回归覆盖没有 WebView 时恢复元数据、项目/任务隔离、审批解决、任务删除、窗口重连、Provider 重启、任务数量/字节预算与超大字段。
前端回归验证原生用量覆盖旧值，以及原生 `null` 清除过期用量。
失败终态回归覆盖事件补齐、明确终态错误优先、成功/重试清理、延迟启动、新轮隔离、无 WebView 的快照恢复、身份隔离和数量/字节预算；前端验证不再从旧 Store 推断终态错误。
2026-09-12 第六批迁移的前端及供应链检查通过：330 项前端测试、Modern/Legacy 构建、类型检查和体积预算通过。合成补丁截断及反斜杠正文回归修正后，Rust 再次完整验证：454 项单元测试、6 项集成测试通过，7 项默认忽略；格式检查与 Clippy 通过。
额外验证 Chromium/WebKit 下补丁直传、Legacy 实际解析渲染、操作分组及关键操作共 12 项浏览器测试，以及真实 Codex 0.154.0 私有安装与 app-server 生命周期测试。Modern 渲染器使用替身核对接收到的完整正文，另以真实 `@pierre/diffs` 解析器核对新增、删除和无 hunk 片段的 3 份规范化样例；这些结果不等于真实 WebView 性能实测。
既有 3 项 Rust 性能基线通过，但未测量本次 Diff 迁移的性能收益。真实原生 WebView 的完整销毁重建交互和性能对比需要另行实测，不能以单元测试代替。

2026-09-13 第七批队列移动迁移的 `pnpm check` 通过：331 项前端测试、Modern/Legacy 构建、类型检查、体积预算及命令授权检查通过；461 项 Rust 单元测试、6 项集成测试、3 项既有性能基线、格式检查与 Clippy 通过，另有 7 项测试默认忽略。定向回归覆盖 7 项原生队列移动测试，以及 Chromium/WebKit 共 8 项队列交互测试。真实 Codex 0.154.0 私有安装和 app-server 生命周期测试通过，但未执行真实队列并发交互或测量本次移动延迟。

2026-09-13 第八批队列读取迁移的 `pnpm check` 通过：332 项前端测试、468 项 Rust 单元测试、6 项集成测试和 3 项既有性能基线通过，另有 7 项测试默认忽略；Modern/Legacy 构建、类型检查、格式检查、Clippy 和体积预算通过。新增 7 项 Rust 队列读取与编辑状态回归，协议及客户端定向测试共 11 项通过，Chromium/WebKit 队列交互共 8 项通过。真实 Codex 0.154.0 生命周期测试通过；完整读取的并发一致性与真实 WebView 性能仍未实测。
